<UserControl x:Class="GUI.Items.Framework.OpenSavePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.Items.Framework"
             mc:Ignorable="d" >
   <Grid>
      <Grid.ColumnDefinitions>
         <ColumnDefinition />
         <ColumnDefinition />
         <ColumnDefinition />
      </Grid.ColumnDefinitions>
      <Button  Grid.Column="0" Content="Save"
               Command = "{Binding SaveCommand}"
               Style="{StaticResource BaseStyle}" Margin="0,0,7,0"
               Template="{StaticResource NewButtonControlTemplate}"/>
      <Button  Grid.Column="1" Content="Save as"
               Command = "{Binding SaveAsCommand}"
               Style="{StaticResource BaseStyle}" Margin="0,0,7,0"
               Template="{StaticResource NewButtonControlTemplate}"/>
      <Button  Grid.Column="2" Content="Create new"
               Command = "{Binding CreateCommand}"
               Style="{StaticResource BaseStyle}" Margin="0,0,7,0"
               Template="{StaticResource NewButtonControlTemplate}"/>
   </Grid>

</UserControl>
