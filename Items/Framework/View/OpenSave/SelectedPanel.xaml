<UserControl x:Class="GUI.Items.Framework.SelectedPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GUI.Items.Framework"
             mc:Ignorable="d">
   <Grid>
      <Grid.ColumnDefinitions>
         <ColumnDefinition Width="68"/>
         <ColumnDefinition/>
         <ColumnDefinition  Width="340"/>
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Column="0" Text="Project File :" Style="{StaticResource TextBlockStyle}"
            HorizontalAlignment="Left"/>
      <TextBlock Grid.Column="1" x:Name="SelectedMorph" Text="{Binding SelectedProjectFileView}" Style="{StaticResource TextBlockStyle}"
            HorizontalAlignment="Center"/>
      <UserControl Grid.Column="2" x:Name="OpenSaveButtons" HorizontalAlignment="Right"/>
   </Grid>
</UserControl>
